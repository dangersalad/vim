###Vim Config

    $ git clone https://github.com/dangersalad/vim.git .vim

After cloning run

    $ cd ~/.vim
    $ git submodule init
    $ git submodule update
    $ cd ~/
    $ ln -s .vim/vimrc .vimrc
